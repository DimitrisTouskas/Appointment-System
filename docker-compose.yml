services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html/appointment-system
    environment:
      DB_HOST: db
    depends_on:
      - db
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: schemaDB
    volumes:
      - ./database/schemaDB.sql:/docker-entrypoint-initdb.d/schemaDB.sql
      - ./database/schemaDB_test.sql:/docker-entrypoint-initdb.d/schemaDB_test.sql